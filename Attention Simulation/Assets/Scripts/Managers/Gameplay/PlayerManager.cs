using UnityEngine;

namespace Emyra.Simulator
{

    /// <summary>
    /// Class to manage player's current stats (energy lvl, spoons, stress)
    /// School stats: subject knowledge lvl, current grades, current assignments - maybe put in a separate class?
    /// </summary>
    public class PlayerManager : MonoBehaviour
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
